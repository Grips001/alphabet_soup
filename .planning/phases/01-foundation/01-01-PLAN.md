---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - index.html
  - src/main.ts
  - src/game/config.ts
  - src/game/constants.ts
  - src/game/systems/TickEngine.ts
  - src/game/systems/TickEngine.test.ts
  - src/game/world/Grid.ts
  - src/game/world/Grid.test.ts
  - src/vite-env.d.ts
autonomous: true
requirements:
  - SIM-01
  - GRID-01
  - INFRA-01

must_haves:
  truths:
    - "TickEngine fires exactly the correct number of ticks for any given accumulated delta"
    - "TickEngine caps accumulator to prevent spiral of death on tab-resume"
    - "Grid data model stores tile occupancy and converts pixel-to-tile coordinates correctly"
    - "Project builds with bun and Vite without errors"
    - "All tests pass via bun run test"
  artifacts:
    - path: "src/game/systems/TickEngine.ts"
      provides: "Fixed-timestep simulation accumulator"
      exports: ["TickEngine"]
    - path: "src/game/systems/TickEngine.test.ts"
      provides: "TickEngine unit tests"
      min_lines: 40
    - path: "src/game/world/Grid.ts"
      provides: "Grid data model with coordinate helpers"
      exports: ["Grid"]
    - path: "src/game/world/Grid.test.ts"
      provides: "Grid unit tests"
      min_lines: 30
    - path: "src/game/constants.ts"
      provides: "Shared constants for tile size, world size, tick rate, zoom range"
      exports: ["TILE_SIZE", "WORLD_TILES", "WORLD_SIZE", "TICK_RATE", "ZOOM_MIN", "ZOOM_MAX"]
    - path: "vite.config.ts"
      provides: "Vite config with Vitest test configuration"
    - path: "package.json"
      provides: "Project dependencies and scripts"
  key_links:
    - from: "src/game/systems/TickEngine.ts"
      to: "src/game/constants.ts"
      via: "imports TICK_RATE"
      pattern: "import.*TICK_RATE.*constants"
    - from: "src/game/world/Grid.ts"
      to: "src/game/constants.ts"
      via: "imports TILE_SIZE, WORLD_TILES"
      pattern: "import.*TILE_SIZE.*constants"
---

<objective>
Bootstrap the AlphabetSoup project and implement the two core pure-TypeScript systems: the fixed-timestep tick engine and the grid data model. Both are testable without Phaser.

Purpose: Establish project scaffold with all dependencies, build/test infrastructure, and the simulation backbone that all future phases depend on.
Output: A buildable Vite + Phaser + TypeScript project with tested TickEngine and Grid classes.
</objective>

<execution_context>
@C:/Users/jdvis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jdvis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + Phaser + TypeScript project</name>
  <files>package.json, tsconfig.json, vite.config.ts, index.html, src/main.ts, src/game/config.ts, src/game/constants.ts, src/vite-env.d.ts</files>
  <action>
Initialize the project using the official Phaser Vite TS template structure (but manually, since we need bun and specific config):

1. Create `package.json` with:
   - Dependencies: `phaser` (^3.90.0)
   - Dev dependencies: `typescript` (~5.7), `vite` (~6.3), `vitest` (latest)
   - Scripts: `dev`, `build` (tsc --noEmit && vite build), `type-check` (tsc --noEmit), `test` (vitest run), `test:watch` (vitest), `preview` (vite preview)
   - Run `bun install` after creating

2. Create `tsconfig.json`:
   - target: ES2020, module: ESNext, moduleResolution: bundler
   - strict: true, esModuleInterop: true, skipLibCheck: true
   - include: ["src"]

3. Create `vite.config.ts`:
   - Import defineConfig from vite
   - Add test config section for vitest: `{ test: { globals: true, environment: 'node' } }`
   - No special plugins needed for Phaser

4. Create `index.html`:
   - Minimal HTML with a `<div id="game-container">` and `<script type="module" src="/src/main.ts">`
   - No body margin/padding (CSS reset for game canvas)

5. Create `src/vite-env.d.ts` with `/// <reference types="vite/client" />`

6. Create `src/game/constants.ts`:
   ```typescript
   export const TILE_SIZE = 32;
   export const WORLD_TILES = 64;
   export const WORLD_SIZE = TILE_SIZE * WORLD_TILES; // 2048
   export const TICK_RATE = 15; // ticks per second
   export const ZOOM_MIN = 0.5;
   export const ZOOM_MAX = 3.0;
   export const CAMERA_ACCELERATION = 0.08;
   export const CAMERA_DRAG = 0.003;
   export const CAMERA_MAX_SPEED = 0.5;
   export const ZOOM_LERP = 0.1;
   ```

7. Create `src/game/config.ts` with Phaser game config:
   - type: Phaser.AUTO, parent: 'game-container'
   - pixelArt: true, roundPixels: true (per research pitfall #3)
   - scale: { mode: Phaser.Scale.RESIZE, autoCenter: Phaser.Scale.CENTER_BOTH }
   - backgroundColor: '#2d2a23' (warm dark tone)
   - Scene array empty for now (wired in Plan 02)

8. Create `src/main.ts`:
   - Import Phaser and gameConfig
   - Create new Phaser.Game(gameConfig)
   - Export the game instance

Run `bun install` then `bun run build` to verify the scaffold compiles.
  </action>
  <verify>
    <automated>cd /c/Users/jdvis/Documents/Projects/alphabet_soup && bun install && bun run type-check</automated>
  </verify>
  <done>Project initializes with bun, TypeScript compiles without errors, Vite config is valid</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TickEngine with TDD</name>
  <files>src/game/systems/TickEngine.ts, src/game/systems/TickEngine.test.ts</files>
  <behavior>
    - Test 1: Constructor sets correct tick rate from ticks-per-second param (default 15 -> 66.67ms per tick)
    - Test 2: update() with delta of exactly 1 tick fires onTick once with tickCount=1
    - Test 3: update() with delta of 2.5 ticks fires onTick twice (accumulator retains remainder)
    - Test 4: update() with delta of 0.5 ticks fires no onTick (accumulator below threshold)
    - Test 5: Multiple update() calls accumulate correctly across frames
    - Test 6: Spiral of death prevention -- delta of 1000 ticks worth caps at maxAccumulator (5 ticks)
    - Test 7: currentTick getter returns cumulative tick count
    - Test 8: alpha getter returns correct interpolation fraction (0-1)
  </behavior>
  <action>
RED: Write test file first at `src/game/systems/TickEngine.test.ts` covering all behaviors above. Use vitest (describe/it/expect). Run tests -- all must fail.

GREEN: Implement `src/game/systems/TickEngine.ts` as a pure TypeScript class (NO Phaser imports):
- Constructor takes `ticksPerSecond` (default: TICK_RATE from constants = 15)
- Computes `tickRate` = 1000 / ticksPerSecond (ms per tick)
- Sets `maxAccumulator` = tickRate * 5 (cap at 5 ticks)
- `update(deltaMs: number, onTick: (tickNumber: number) => void): void` -- accumulator pattern per research
- `get currentTick(): number` -- returns total tick count
- `get alpha(): number` -- returns accumulator / tickRate (for future interpolation)

Run tests -- all must pass.

REFACTOR if needed, ensure tests still pass.
  </action>
  <verify>
    <automated>cd /c/Users/jdvis/Documents/Projects/alphabet_soup && bun run vitest run src/game/systems/TickEngine.test.ts</automated>
  </verify>
  <done>TickEngine class passes all 8+ test cases covering accumulation, tick firing, spiral-of-death cap, and alpha interpolation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Grid data model with TDD</name>
  <files>src/game/world/Grid.ts, src/game/world/Grid.test.ts</files>
  <behavior>
    - Test 1: Constructor creates a width x height grid initialized to empty (0)
    - Test 2: getTile(x, y) returns tile value at grid coordinates
    - Test 3: setTile(x, y, value) sets tile value and getTile retrieves it
    - Test 4: isInBounds(x, y) returns true for valid coordinates, false for out-of-bounds
    - Test 5: pixelToTile(px, py) converts pixel coords to tile coords (floor division by TILE_SIZE)
    - Test 6: tileToPixel(tx, ty) converts tile coords to top-left pixel coords (multiply by TILE_SIZE)
    - Test 7: Out-of-bounds getTile/setTile returns default / is no-op (no crash)
    - Test 8: tileToCenter(tx, ty) returns center pixel of tile (for entity placement)
  </behavior>
  <action>
RED: Write test file first at `src/game/world/Grid.test.ts` covering all behaviors above. Run tests -- all must fail.

GREEN: Implement `src/game/world/Grid.ts` as a pure TypeScript class (NO Phaser imports):
- Constructor takes `width` and `height` (default: WORLD_TILES from constants = 64)
- Internal storage: `Uint8Array` of size width * height (flat 2D array, index = y * width + x)
- `getTile(x: number, y: number): number` -- returns tile type (0 = empty)
- `setTile(x: number, y: number, value: number): void` -- sets tile type
- `isInBounds(x: number, y: number): boolean` -- validates grid coordinates
- `pixelToTile(px: number, py: number): { x: number, y: number }` -- pixel-to-tile conversion using TILE_SIZE
- `tileToPixel(tx: number, ty: number): { x: number, y: number }` -- tile-to-pixel (top-left corner)
- `tileToCenter(tx: number, ty: number): { x: number, y: number }` -- tile center pixel position
- `get width(): number` and `get height(): number` accessors

Run tests -- all must pass.

This class stores game state (what's on each tile). It does NOT render anything. Rendering is handled by GridRenderer in Plan 02. This separation is critical per research anti-patterns: "Storing game state in Phaser objects" is explicitly warned against.
  </action>
  <verify>
    <automated>cd /c/Users/jdvis/Documents/Projects/alphabet_soup && bun run vitest run src/game/world/Grid.test.ts</automated>
  </verify>
  <done>Grid class passes all 8+ test cases covering tile access, bounds checking, and coordinate conversion</done>
</task>

</tasks>

<verification>
All pure-TS systems are tested and the project scaffold builds:
```bash
bun run test && bun run type-check
```
</verification>

<success_criteria>
- Project scaffold exists with Phaser 3.90, TypeScript, Vite, Vitest
- TickEngine passes all unit tests (accumulation, firing, cap, alpha)
- Grid passes all unit tests (access, bounds, coordinate conversion)
- `bun run test` passes, `bun run type-check` passes
- No Phaser imports in TickEngine.ts or Grid.ts (pure TS)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
