---
phase: 03-factory-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/constants.ts
  - src/game/entities/Inserter.ts
  - src/game/entities/Inserter.test.ts
  - src/game/entities/LongArmInserter.ts
  - src/game/entities/LongArmInserter.test.ts
  - src/game/entities/Assembler.ts
  - src/game/entities/Assembler.test.ts
  - src/game/entities/Splitter.ts
  - src/game/entities/Splitter.test.ts
  - src/game/entities/UndergroundBelt.ts
  - src/game/entities/UndergroundBelt.test.ts
  - src/game/entities/WordItem.ts
  - src/game/entities/WordItem.test.ts
  - src/game/entities/BeltItem.ts
  - src/game/entities/Belt.ts
  - src/game/data/wordDictionary.ts
  - src/game/data/wordDictionary.test.ts
autonomous: true
requirements:
  - WORD-01
  - WORD-02
must_haves:
  truths:
    - "All new entity types implement the Building interface with occupiedTiles() and type"
    - "WordRecipe correctly computes letter counts for any word"
    - "Assembler can track ingredient collection and report needed letters"
    - "Word dictionary contains only words using quarry letters E,T,A,O,I,N,S,R,H,L"
    - "Belt.item accepts both LetterItem and WordItem via BeltItem union type"
  artifacts:
    - path: "src/game/entities/Inserter.ts"
      provides: "1x1 inserter entity with direction, transfer timer, held item"
      exports: ["Inserter", "InserterDirection"]
    - path: "src/game/entities/LongArmInserter.ts"
      provides: "1x1 long-arm inserter entity with extended reach"
      exports: ["LongArmInserter"]
    - path: "src/game/entities/Assembler.ts"
      provides: "3x3 assembler entity with recipe, ingredient slots, assembly timer"
      exports: ["Assembler"]
    - path: "src/game/entities/Splitter.ts"
      provides: "1x2 splitter entity with alternating output state"
      exports: ["Splitter"]
    - path: "src/game/entities/UndergroundBelt.ts"
      provides: "Underground belt entry/exit pair entities with tick buffer"
      exports: ["UndergroundEntry", "UndergroundExit"]
    - path: "src/game/entities/WordItem.ts"
      provides: "Word item entity matching LetterItem interface shape"
      exports: ["WordItem"]
    - path: "src/game/entities/BeltItem.ts"
      provides: "Union type for items on belts"
      exports: ["BeltItem"]
    - path: "src/game/data/wordDictionary.ts"
      provides: "Tiered word dictionary with WordRecipe type"
      exports: ["WordRecipe", "WORD_DICTIONARY", "makeRecipe", "getRecipesByTier"]
  key_links:
    - from: "src/game/entities/BeltItem.ts"
      to: "src/game/entities/Belt.ts"
      via: "Belt.item type changed to BeltItem | null"
      pattern: "BeltItem"
    - from: "src/game/data/wordDictionary.ts"
      to: "src/game/entities/Assembler.ts"
      via: "Assembler.recipe uses WordRecipe type"
      pattern: "WordRecipe"
---

<objective>
Create all Phase 3 entity data models, the BeltItem union type, word dictionary, and new constants. This is the foundation layer that all Phase 3 systems and renderers depend on.

Purpose: Every subsequent plan imports types and entities defined here. Building these first with full TDD ensures correct contracts before system logic.
Output: 8 new entity/data files with tests, updated Belt.ts and constants.ts
</objective>

<execution_context>
@C:/Users/jdvis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jdvis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-factory-loop/03-CONTEXT.md
@.planning/phases/03-factory-loop/03-RESEARCH.md

<interfaces>
<!-- Existing interfaces that new entities must conform to -->

From src/game/systems/BuildingSystem.ts:
```typescript
export interface Building {
  type: string;
  occupiedTiles(): Array<{ x: number; y: number }>;
}
```

From src/game/entities/LetterItem.ts:
```typescript
export class LetterItem {
  readonly letter: string;
  tile: { x: number; y: number };
  previousTile: { x: number; y: number };
  snapshotPosition(): void;
}
```

From src/game/entities/Belt.ts:
```typescript
export enum BeltDirection { North = "N", South = "S", East = "E", West = "W" }
export class Belt {
  readonly type = "belt" as const;
  direction: BeltDirection;
  item: LetterItem | null;  // Will change to BeltItem | null
  tileX: number;
  tileY: number;
  occupiedTiles(): Array<{ x: number; y: number }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Entity data models and BeltItem type</name>
  <files>
    src/game/constants.ts,
    src/game/entities/BeltItem.ts,
    src/game/entities/WordItem.ts,
    src/game/entities/WordItem.test.ts,
    src/game/entities/Inserter.ts,
    src/game/entities/Inserter.test.ts,
    src/game/entities/LongArmInserter.ts,
    src/game/entities/LongArmInserter.test.ts,
    src/game/entities/Assembler.ts,
    src/game/entities/Assembler.test.ts,
    src/game/entities/Splitter.ts,
    src/game/entities/Splitter.test.ts,
    src/game/entities/UndergroundBelt.ts,
    src/game/entities/UndergroundBelt.test.ts,
    src/game/entities/Belt.ts
  </files>
  <behavior>
    WordItem:
    - constructor("CAT", {x:5, y:5}) sets word="CAT", tile={x:5,y:5}, previousTile={x:5,y:5}
    - snapshotPosition() deep-copies tile to previousTile

    Inserter:
    - type === "inserter", occupiedTiles() returns single tile [{x, y}]
    - direction cycles through N/E/S/W
    - constructor(direction, tileX, tileY) sets ticksSinceLastTransfer=0, heldItem=null
    - pickupTile()/dropoffTile() return correct adjacent tiles based on direction

    LongArmInserter:
    - type === "long-arm-inserter", occupiedTiles() returns single tile
    - pickupTile(range) returns 1 or 2 tiles away (toggleable)
    - dropoffTile() always returns 2 tiles in output direction

    Assembler:
    - type === "assembler", occupiedTiles() returns 9 tiles (3x3 from anchor)
    - neededCount("E") returns required-collected for recipe letter
    - neededCount returns 0 when no recipe set
    - isReadyToAssemble() returns true only when all letterCounts satisfied
    - isReadyToAssemble() returns false when no recipe set
    - depositLetter("E") increments collected, returns true; rejects unneeded letter returns false
    - collectedIngredients resets via reset()

    Splitter:
    - type === "splitter", occupiedTiles() returns 2 tiles (side-by-side perpendicular to direction)
    - nextOutputSide() alternates "left"/"right"

    UndergroundEntry:
    - type === "underground-entry", occupiedTiles() returns single tile
    - pair reference to UndergroundExit (nullable)
    - buffer array for items in transit with ticksRemaining

    UndergroundExit:
    - type === "underground-exit", occupiedTiles() returns single tile
    - pair reference to UndergroundEntry (nullable)
  </behavior>
  <action>
    Add new constants to constants.ts:
    - TILE_INSERTER = 10, TILE_LONG_ARM_INSERTER = 11, TILE_ASSEMBLER = 12, TILE_SPLITTER = 13, TILE_UNDERGROUND_ENTRY = 14, TILE_UNDERGROUND_EXIT = 15
    - INSERTER_TRANSFER_INTERVAL = 8 (ticks, ~0.5s at 15tps)
    - ASSEMBLER_SIZE = 3 (tiles per side)
    - ASSEMBLY_TICKS_BASE = 10, ASSEMBLY_TICKS_PER_LETTER = 4
    - UNDERGROUND_MAX_DISTANCE = 6 (tiles)
    - DEPTH_INSERTER_ARM = 25 (between items and ghost)

    Create BeltItem.ts:
    ```typescript
    export type BeltItem = LetterItem | WordItem;
    ```

    Update Belt.ts: change `item: LetterItem | null` to `item: BeltItem | null` and update import.

    Create all entity classes following the Building interface pattern. Each entity is pure TS (no Phaser imports). Use BeltDirection from Belt.ts for direction fields. Inserter direction is a separate enum InserterDirection with N/E/S/W since inserters have distinct pickup/dropoff semantics vs belt flow direction.

    Assembler uses Map<string, number> for collectedIngredients and references WordRecipe type from wordDictionary.ts (import type only). The recipe field is `WordRecipe | null`. Assembler has:
    - assemblyTicksRemaining: number (0 when idle)
    - outputItem: WordItem | null (set when assembly completes)
    - neededCount(letter): number
    - isReadyToAssemble(): boolean
    - depositLetter(letter): boolean (returns false if not needed)
    - reset(): void (clears ingredients, timer, output)

    Splitter occupiedTiles: for direction N or S, tiles are at (x, y) and (x+1, y). For direction E or W, tiles are at (x, y) and (x, y+1). The perpendicular pair aligns with the split/merge axis.

    UndergroundEntry has buffer: Array<{item: BeltItem, ticksRemaining: number}>. UndergroundExit has no buffer. Both hold `pair` reference to each other.

    Write all tests first (RED), then implement (GREEN). Follow existing test patterns in src/game/entities/ and src/game/systems/.
  </action>
  <verify>
    <automated>bun test -- Inserter Assembler Splitter UndergroundBelt WordItem</automated>
  </verify>
  <done>All 6 entity types created with passing tests. Belt.item uses BeltItem union. Constants added. No Phaser imports in any entity file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Word dictionary data file</name>
  <files>
    src/game/data/wordDictionary.ts,
    src/game/data/wordDictionary.test.ts
  </files>
  <behavior>
    - WordRecipe type has word, tier (1|2|3), letterCounts (Map), uniqueLetterCount
    - makeRecipe("SEE", 1) produces {word:"SEE", tier:1, letterCounts:{S:1, E:2}, uniqueLetterCount:2}
    - WORD_DICTIONARY is Array of WordRecipe
    - All tier 1 words are 3 letters, tier 2 are 4 letters, tier 3 are 5 letters
    - All words use only letters E,T,A,O,I,N,S,R,H,L
    - getRecipesByTier(1) returns only tier 1 recipes
    - Each word appears exactly once in the dictionary
    - Tier 1 sorted by uniqueLetterCount ascending (fewer unique = easier first)
    - Dictionary contains at least 30 tier 1 words, 40 tier 2, 30 tier 3
  </behavior>
  <action>
    Create WordRecipe interface and makeRecipe helper function. Curate a word dictionary of ~100+ common English words using only the 10 quarry letters (E, T, A, O, I, N, S, R, H, L).

    Tier 1 (3-letter): Words like SEE, TEE, TOE, TEN, TIN, TAN, SON, SIT, HIT, HOT, NOT, NIT, NET, SET, LET, LOT, LIT, ATE, EAT, OAT, TEA, THE, SHE, HIS, HER, INN, ALL, ILL, OLE, ORE, ARE, ART, etc. Include repeating-letter words (SEE, TOO, ALL) as easiest within tier.

    Tier 2 (4-letter): Words like TONE, NOSE, ROSE, TIRE, HIRE, HOSE, TILE, LINE, LONE, LORE, SORE, TORE, TORN, HORN, SORT, SLOT, SNOT, RIOT, INTO, SENT, LINT, HINT, THIN, THEN, SHIN, NINE, NOTE, RANT, RAIN, RENT, REIN, TALE, SALT, TALL, ROLL, TOLL, LION, IRON, etc.

    Tier 3 (5-letter): Words like STONE, STORE, STOLE, STERN, HORSE, SHORE, THOSE, THEIR, LINER, LONER, SNORE, ONSET, ROAST, TOAST, LEAST, STEAL, TALES, STAIR, TRAIN, NORTH, THORN, SHORT, SHIRT, NOISE, RESIN, RISEN, RINSE, INERT, INTER, RATIO, etc.

    Sort each tier by uniqueLetterCount ascending. Export WORD_DICTIONARY array and getRecipesByTier(tier) helper. Validate every word only uses quarry letters by checking each character.

    Write tests first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>bun test wordDictionary</automated>
  </verify>
  <done>Word dictionary with 100+ words across 3 tiers, all using only quarry letters, sorted by difficulty within tier. makeRecipe and getRecipesByTier exported and tested.</done>
</task>

</tasks>

<verification>
- `bun test` passes (all new entity + dictionary tests)
- `bun run type-check` passes (no type errors from BeltItem change)
- No Phaser imports in any file under entities/ or data/
- Belt.ts uses BeltItem type for item field
</verification>

<success_criteria>
All Phase 3 entity contracts are defined and tested. Belt.item accepts WordItem. Word dictionary provides recipes for assemblers. Every subsequent plan can import these types without modification.
</success_criteria>

<output>
After completion, create `.planning/phases/03-factory-loop/03-01-SUMMARY.md`
</output>
