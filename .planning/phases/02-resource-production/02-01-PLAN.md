---
phase: 02-resource-production
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/constants.ts
  - src/game/entities/Quarry.ts
  - src/game/entities/Quarry.test.ts
  - src/game/entities/Belt.ts
  - src/game/entities/Belt.test.ts
  - src/game/entities/LetterItem.ts
  - src/game/entities/LetterItem.test.ts
  - src/game/systems/BuildingSystem.ts
  - src/game/systems/BuildingSystem.test.ts
  - src/game/world/quarryLayout.ts
autonomous: true
requirements: [GRID-02, GRID-03]

must_haves:
  truths:
    - "Building placement can be validated against tile occupancy"
    - "Quarry entity occupies 2x2 tiles and knows its output tile"
    - "Belt entity has a direction and computable variant"
    - "LetterItem snapshots position for interpolation"
    - "Demolishing a multi-tile building removes all occupied tiles"
  artifacts:
    - path: "src/game/entities/Quarry.ts"
      provides: "Quarry data model with 2x2 occupancy, letter, output direction, production timer"
      exports: ["Quarry"]
    - path: "src/game/entities/Belt.ts"
      provides: "Belt data model with direction, variant, item slot"
      exports: ["Belt", "BeltDirection", "BeltVariant", "computeBeltVariant"]
    - path: "src/game/entities/LetterItem.ts"
      provides: "LetterItem with tile position, previousTile for interpolation"
      exports: ["LetterItem"]
    - path: "src/game/systems/BuildingSystem.ts"
      provides: "Building registry with place/demolish/getAt operations"
      exports: ["BuildingSystem", "Building"]
    - path: "src/game/world/quarryLayout.ts"
      provides: "Fixed quarry positions for 64x64 map"
      exports: ["QUARRY_DEFINITIONS"]
    - path: "src/game/constants.ts"
      provides: "Phase 2 constants (tile types, production interval, belt speed, depths, item size, letter colors)"
  key_links:
    - from: "src/game/systems/BuildingSystem.ts"
      to: "src/game/entities/Quarry.ts"
      via: "Building interface implemented by Quarry"
      pattern: "occupiedTiles"
    - from: "src/game/entities/Belt.ts"
      to: "src/game/entities/LetterItem.ts"
      via: "Belt.item slot holds a LetterItem or null"
      pattern: "item.*LetterItem"
---

<objective>
Create all pure TypeScript entity data models and the BuildingSystem registry for Phase 2. This establishes the type contracts and data structures that all subsequent plans build on.

Purpose: Every system (BeltSystem, QuarrySystem, PlacementSystem) and every renderer depends on these entity types. Getting the data model right prevents cascading rewrites.
Output: Tested entity classes (Quarry, Belt, LetterItem), BuildingSystem with place/demolish, quarry layout data, and all Phase 2 constants.
</objective>

<execution_context>
@C:/Users/jdvis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jdvis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-resource-production/02-CONTEXT.md
@.planning/phases/02-resource-production/02-RESEARCH.md

@src/game/constants.ts
@src/game/world/Grid.ts

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/game/constants.ts:
```typescript
export const TILE_SIZE = 32;
export const WORLD_TILES = 64;
export const WORLD_SIZE = TILE_SIZE * WORLD_TILES;
export const TICK_RATE = 15;
```

From src/game/world/Grid.ts:
```typescript
export class Grid {
  get width(): number;
  get height(): number;
  isInBounds(x: number, y: number): boolean;
  getTile(x: number, y: number): number;
  setTile(x: number, y: number, value: number): void;
  pixelToTile(px: number, py: number): { x: number; y: number };
  tileToPixel(tx: number, ty: number): { x: number; y: number };
  tileToCenter(tx: number, ty: number): { x: number; y: number };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Constants, entity data models, and quarry layout</name>
  <files>
    src/game/constants.ts,
    src/game/entities/Quarry.ts,
    src/game/entities/Quarry.test.ts,
    src/game/entities/Belt.ts,
    src/game/entities/Belt.test.ts,
    src/game/entities/LetterItem.ts,
    src/game/entities/LetterItem.test.ts,
    src/game/world/quarryLayout.ts
  </files>
  <behavior>
    Quarry:
    - Test: Quarry("E", {x:4, y:4}, "S") has letter "E", originTile {4,4}
    - Test: occupiedTiles() returns 4 tiles for 2x2 block: (4,4), (5,4), (4,5), (5,5)
    - Test: outputTile for direction "S" is one tile below the 2x2 block (correct adjacent tile based on output direction)
    - Test: production timer starts at 0, canProduce() false initially
    - Test: advancing timer to QUARRY_PRODUCTION_INTERVAL makes canProduce() true
    - Test: resetTimer() resets to 0
    - Test: backpressure flag blocks production

    Belt:
    - Test: Belt("N") has direction North, item is null
    - Test: Belt can hold a LetterItem in its item slot
    - Test: computeBeltVariant("N", "N") returns StraightNS
    - Test: computeBeltVariant("E", "E") returns StraightEW
    - Test: computeBeltVariant("S", "E") returns correct corner variant
    - Test: computeBeltVariant for all 4 corner cases returns correct variant
    - Test: All straight directions map correctly (N/S -> StraightNS, E/W -> StraightEW)

    LetterItem:
    - Test: LetterItem("A", {x:5, y:5}) has letter "A", tile and previousTile both at (5,5)
    - Test: snapshotPosition() copies current tile to previousTile
    - Test: After moving tile to (6,5) and snapshotPosition(), previousTile is (6,5)
    - Test: Mutating tile after snapshot does not change previousTile (deep copy)
  </behavior>
  <action>
    1. Add Phase 2 constants to constants.ts:
       - Tile type codes: TILE_EMPTY=0, TILE_QUARRY=1, TILE_BELT_N=2, TILE_BELT_S=3, TILE_BELT_E=4, TILE_BELT_W=5, TILE_BELT_CORNER_NE=6, TILE_BELT_CORNER_NW=7, TILE_BELT_CORNER_SE=8, TILE_BELT_CORNER_SW=9
       - QUARRY_PRODUCTION_INTERVAL=30 (ticks, ~2s at 15tps)
       - BELT_SPEED=1 (tiles per tick)
       - ITEM_SIZE=16 (pixels, half of TILE_SIZE)
       - Depth layers: DEPTH_GROUND=0, DEPTH_GRID_LINES=1, DEPTH_BUILDINGS=10, DEPTH_ITEMS=20, DEPTH_GHOST=30, DEPTH_UI=1000
       - LETTER_COLORS: Record<string, number> with 26 warm-toned hex colors, distinct per letter, high contrast for dark text readability. Use a cohesive palette (e.g., muted warm tones: amber, terracotta, sage, slate, dusty rose, etc.)

    2. Create src/game/entities/ directory.

    3. Create LetterItem.ts (pure TS, no Phaser):
       - Properties: letter (readonly string), tile ({x, y}), previousTile ({x, y})
       - Constructor: takes letter and startTile, sets both tile and previousTile to copies of startTile
       - snapshotPosition(): copies current tile to previousTile (deep copy, not reference)

    4. Create Belt.ts (pure TS, no Phaser):
       - BeltDirection enum: North, South, East, West (string values "N","S","E","W")
       - BeltVariant enum: StraightNS, StraightEW, CornerNE, CornerNW, CornerSE, CornerSW
       - Belt class: direction (BeltDirection), variant (BeltVariant), item (LetterItem | null), tileX, tileY
       - computeBeltVariant(inputDir, outputDir) function: maps direction pairs to variants using a lookup table
       - Export all types

    5. Create Quarry.ts (pure TS, no Phaser):
       - Properties: letter (string), originTile ({x,y}), outputDirection (BeltDirection), productionTimer (number), backpressured (boolean)
       - occupiedTiles(): returns array of 4 tile positions for the 2x2 block
       - outputTile: computed property returning the tile adjacent to the quarry body in the output direction. For "S": tile at (originX, originY+2). For "N": (originX, originY-1). For "E": (originX+2, originY). For "W": (originX-1, originY). Pick a consistent reference point within the 2x2 block.
       - tickProduction(): increments timer, returns void
       - canProduce(): returns true when timer >= QUARRY_PRODUCTION_INTERVAL and not backpressured
       - resetTimer(): sets timer to 0

    6. Create quarryLayout.ts (pure data):
       - Export QUARRY_DEFINITIONS array with 10 quarries for letters E, T, A, O, I, N, S, R, H, L
       - Scattered positions across the 64x64 map for routing challenges
       - Each entry: { letter, origin: {x, y}, output: BeltDirection }
       - Ensure no quarry 2x2 footprint overlaps or goes out of bounds

    7. Write all test files FIRST (RED), then implement (GREEN). Follow project TDD convention.
  </action>
  <verify>
    <automated>bun test src/game/entities/Quarry.test.ts src/game/entities/Belt.test.ts src/game/entities/LetterItem.test.ts</automated>
  </verify>
  <done>All entity tests pass. Quarry has 2x2 occupancy and production timer. Belt has direction/variant/item slot. LetterItem has interpolation snapshot. Constants file has all Phase 2 values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BuildingSystem registry with place and demolish</name>
  <files>
    src/game/systems/BuildingSystem.ts,
    src/game/systems/BuildingSystem.test.ts
  </files>
  <behavior>
    - Test: place() a Belt at (5,5) -> getAt(5,5) returns that Belt
    - Test: place() returns true on empty tile, false on occupied tile
    - Test: place() a Quarry at (4,4) -> getAt returns the Quarry for all 4 tiles (4,4), (5,4), (4,5), (5,5)
    - Test: place() Quarry at (4,4) then Belt at (5,4) returns false (tile occupied by quarry)
    - Test: demolish(5,4) where a Quarry occupies (4,4)-(5,5) removes ALL 4 tiles
    - Test: demolish() returns the removed building entity
    - Test: demolish() on empty tile returns null
    - Test: getAt() returns null for empty tiles
    - Test: getAllBuildings() returns unique building instances (quarry counted once, not 4 times)
    - Test: After demolish, getAt returns null for all previously occupied tiles
  </behavior>
  <action>
    1. Define a Building interface that Quarry and Belt both implement:
       - occupiedTiles(): Array<{x: number, y: number}>
       - type: string (e.g., "quarry" or "belt")

    2. Create BuildingSystem (pure TS, no Phaser):
       - Private Map<string, Building> keyed by "x,y" tile coordinate strings
       - place(entity: Building): boolean — registers all occupiedTiles(), returns false if any tile is already occupied (does NOT partially place)
       - demolish(tx, ty): Building | null — looks up entity at tile, removes ALL of its occupiedTiles, returns the entity
       - getAt(tx, ty): Building | null — lookup by tile
       - getAllBuildings(): Building[] — returns unique building instances (deduplicated)
       - tileKey(x, y) helper function

    3. Ensure Quarry and Belt implement the Building interface. If needed, add the `type` property and `occupiedTiles()` method to Belt (Belt occupies 1 tile: [{x: tileX, y: tileY}]).

    4. Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>bun test src/game/systems/BuildingSystem.test.ts</automated>
  </verify>
  <done>BuildingSystem correctly registers single-tile (Belt) and multi-tile (Quarry) buildings. Place rejects occupied tiles. Demolish removes all tiles of multi-tile buildings. All tests pass.</done>
</task>

</tasks>

<verification>
- `bun test` — all new tests pass alongside existing Phase 1 tests
- `bun run build` — TypeScript compiles with no errors
- No Phaser imports in any entities/ or systems/ file created in this plan
</verification>

<success_criteria>
- Quarry, Belt, LetterItem entity classes exist with full test coverage
- BuildingSystem handles place/demolish for single-tile and multi-tile buildings
- All Phase 2 constants defined in constants.ts
- Quarry layout defines 10 quarry positions
- All tests pass, no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-resource-production/02-01-SUMMARY.md`
</output>
