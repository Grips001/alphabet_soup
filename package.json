{
  "name": "alphabet-soup",
  "version": "0.1.0",
  "description": "AlphabetSoup - A factory automation word game built with Phaser 3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "~5.7.0",
    "vite": "~6.3.0",
    "vitest": "^3.0.0"
  }
}
